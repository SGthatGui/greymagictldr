import Link from 'next/link';

const Navbar = () => {
  return (
    <div>
      <nav>
        <Link href='/'>
          <h1>{`{TS;IR}`}</h1>
        </Link>
        <br />
      </nav>
    </div>
  );
};

export default Navbar;
